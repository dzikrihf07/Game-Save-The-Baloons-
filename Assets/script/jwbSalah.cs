using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class jwbSalah : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void kurang() {
        blNgurangScript.health -= 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}